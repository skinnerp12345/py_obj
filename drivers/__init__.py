"""Standalone driver scripts, each independently runnable, each reading only
the config section(s) it needs from one shared python_obj/configs/config.yaml (see
python_obj.config). Not imported by the rest of python_obj/ -- these are
entry points, not library code other modules depend on.
"""
